---
description: "프로덕션 릴리즈 전 폴리싱 (solo). Claude 실행자+검증자 역할 전환"
argument-hint: [기획 정의 문서 경로]
---

# Polish for Release Solo (→ /polish-for-release --mode solo)

이 명령은 `/polish-for-release --mode solo`의 별칭입니다.

`$ARGUMENTS`를 `--mode solo`와 함께 `/polish-for-release`로 전달하여 실행합니다.

```
Read ${CLAUDE_PLUGIN_ROOT}/commands/polish-for-release.md
```

위 파일의 지침을 `--mode solo`로 설정하여 따릅니다.
- Claude 단독 실행자+검증자 역할 전환
- 외부 AI 불필요
